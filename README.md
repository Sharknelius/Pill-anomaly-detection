Pill Detection training and evalutation repo.

train.py is ran for any training and saving of the model.

faster_rcnn.py is where the model creation code is.

test.py is ran for testing the model.

dataset.py is for loading and parsing datasets. 